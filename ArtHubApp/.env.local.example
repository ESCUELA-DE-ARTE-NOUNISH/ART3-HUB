# Pinata Gateway Token (Optional - get from https://app.pinata.cloud/gateway)
# This helps avoid rate limiting on IPFS image requests in the gallery
# NEXT_PUBLIC_PINATA_GATEWAY_TOKEN=your_gateway_token_here
